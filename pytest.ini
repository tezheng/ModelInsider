[tool:pytest]
# Main pytest configuration for modelexport testing

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output configuration
addopts = 
    -v 
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
    --durations=10

# Markers for test categorization
markers =
    unit: Unit tests for individual components
    integration: Integration tests across multiple components
    strategy: Strategy-specific tests
    fx: FX strategy tests
    htp: HTP strategy tests  
    usage_based: Usage-based strategy tests
    cli: CLI integration tests
    slow: Tests that take longer to run
    requires_transformers: Tests that require transformers library
    requires_onnx: Tests that require ONNX runtime

# Minimum version requirements
minversion = 6.0

# Test timeout (in seconds)
timeout = 300

# Parallel execution
# addopts = -n auto  # Uncomment to enable parallel testing with pytest-xdist

# Coverage configuration (if using pytest-cov)
# addopts = --cov=modelexport --cov-report=html --cov-report=term-missing

# Warnings filters
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning